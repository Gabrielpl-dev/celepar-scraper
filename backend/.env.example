# Copiar pra backend/.env (gitignored) e preencher os valores reais.
# Nunca commitar backend/.env com valores reais.

PORT=3000

# Gerar com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Oracle REAG (banco institucional) -- connection string completa no cofre de senhas
ORACLE_USER=
ORACLE_PASSWORD=
ORACLE_CONNECT_STRING=

# Portal Agrofit/Embrapa
AGROFIT_USER=
AGROFIT_PASSWORD=
AGROFIT_KEY=
AGROFIT_SECRET=

# Usados pela extração de bulas (backend/lib/deepseekClient.js, cerebrasClient.js)
DEEPSEEK_API_KEY=
CEREBRAS_API_KEY=
